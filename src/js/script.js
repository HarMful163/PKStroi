'use strict'

window.addEventListener('DOMContentLoaded', function() {
    
    // const link = document.querySelectorAll('.menu__link'),
    //       subLink = document.querySelectorAll('.menu__sublink');

    // link.forEach((item, i) => {
        
    // });

});